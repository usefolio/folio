from setuptools import setup, find_packages

setup(
    name="folio-utils-usage-cop",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # Dependencies managed via requirements.txt
    python_requires=">=3.8",
)