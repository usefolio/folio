# README Assets

This folder holds images and GIFs referenced by the repository root `README.md`.

Guidelines:
- Keep filenames stable once linked in `README.md`.
- Prefer descriptive names like `upload-corpus.png` and `tabular-review.gif`.
- If assets move to a CDN later, replace paths in `README.md` while keeping this folder as a local fallback.
