## Export Modal Config

- Uses the shared `Checkbox` component from `src/components/ui/checkbox.tsx` for column and view selectors so the square styling stays consistent across the app.
- Avoid overriding the checkbox radius in this modal; stick with the default `rounded-none` style defined on the shared component.
