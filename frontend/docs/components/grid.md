## Column Visibility Manager

The column visibility menu (accessed via the filter icon on the grid header) now shows a small grey status line at the top that lists the total number of columns in the sheet. This gives users quick context before they start hiding or showing individual columns.

